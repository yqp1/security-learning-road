# 第二章 变量和简单数据类型 - 我的个人笔记

> 整理人：我
> 
> 
> 日期：2026年6月6日
状态：刚入门时写的，有些地方当时有点懵
不知道怎么使用Notion导入进来直接复制的，这是我的链接：https://www.notion.so/03-Python-ca322d73fb304369adc2b9ac9cfe8056?source=copy_link
## 1. 变量

变量就是个**标签**，你给它贴个名字，它帮你存东西。

```python
message = "Hello world"
print(message)
```

**几条规矩**（背下来，不然会报错）：

- 变量名只能包含字母、数字、下划线，**不能数字开头**
- 不能有空格，用下划线代替（比如 `my_name`，不是 `my name`）
- 不能用Python关键字（比如 `print`、`if`、`for` 这些）
- 尽量起有意义的名字，别用 `a` `b` `c`，过两天自己都看不懂

我刚开始犯过这种错：`2things = 5` 直接报错，还以为Python歧视我。

## 2. 字符串

字符串就是**引号包起来的一串东西**，单引号双引号都行，但要成对。

```python
"This is a string."
'This is also a string.'
```

**常用操作**（一定要动手敲，不要光看）：

### 大小写方法

```python
name = "liu bei"
print(name.title())   # Liu Bei  每个单词首字母大写
print(name.upper())   # LIU BEI
print(name.lower())   # liu bei
```

`.title()` 一开始老记不住，后来发现宴会座位牌上名字都是这样，就记住了。

### 拼接（f-string 和 传统方法）

```python
first = "zhang"
last = "fei"
full = f"{first} {last}"      # 推荐，Python 3.6以后
print(full)   # zhang fei

# 老写法
full = first + " " + last
```

f-string 前面那个 `f` 不能少，我漏过好几次，结果直接输出 `{first} {last}` 无语。

### 换行和制表符

```python
print("Languages:\\n\\tPython\\n\\tC\\n\\tJavaScript")
```

`\\n` 换行，`\\t` 空几个格（好像8个空格？），自己试一次就知道了。

### 去掉空白

```python
name = "  liu bei  "
print(name.lstrip())   # 去掉左边空格
print(name.rstrip())   # 去掉右边空格
print(name.strip())    # 两边都去
```

这个在输入处理时很有用，比如用户不小心打了空格。

## 3. 数字

### 整数

加减乘除和取模：`+ - * / ** %`

```python
2 + 3     # 5
3 - 2     # 1
2 * 3     # 6
3 / 2     # 1.5  (注意，结果是浮点数)
3 // 2    # 1   整除
3 ** 2    # 9   平方
3 % 2     # 1   余数
```

运算顺序和数学一样，括号优先。

### 浮点数

带小数点的数，比如 `0.1 + 0.2` 有时候会得到 `0.30000000000000004`，这不是bug，是计算机存小数的方式问题。练习题里遇到不用慌，后面会讲怎么处理。

### 下划线在数字里

可以写 `14_000_000`，Python会忽略下划线，就是 `14000000`，为了好看。

### 同时赋值

```python
x, y, z = 1, 2, 3
```

一行搞定，但别滥用，会看不懂。

## 4. 常量

Python没有真正的常量，但约定**全大写变量名**表示常量：

```python
MAX_SIZE = 100
```

意思是“你不要改它”，但改了也不会报错，全靠自觉。

## 5. 注释

用 `#`开头，解释代码是干啥的。

```python
# 计算圆的面积
area = 3.14 * r ** 2
```

**好注释**：解释“为什么”，不是重复代码在做什么（比如 `i = i + 1  # 加1` 这种废话）。

我一开始懒得写，后来回看代码自己都不懂，现在强制自己每个函数至少一行注释。

## 6. 避坑小记录

- 字符串里有引号怎么办？
    
    ```python
    message = 'I'm Liu Bei'   # 报错！因为中间单引号结束了字符串
    message = "I'm Liu Bei"   # 正确
    message = 'I\\'m Liu Bei'  # 用转义也行，但麻烦
    ```
    
- 数字转字符串才能拼接：
    
    ```python
    age = 23
    message = "Happy " + age + "rd birthday"   # 报错，类型不同
    message = f"Happy {age}rd birthday"        # 正确，用f-string
    ```
    
- `print` 里如果有 `end` 参数可以控制换行，不过第二章没怎么提，我提前看到了。

## 7. 我的错误地方

第二章后面的练习像是“把名字存在变量里然后打印各种格式”，我做了好几遍，尤其那个 `print(f"{name} once said, \\"...\\"")` 引号套引号折腾了一会。

还有数字的练习，那个 `8` 的各种组合（加减乘除）我一开始只想到 `4+4`，后来才写全。

## 8. 小结

- 变量名要**见名知意**，别偷懒
- 字符串方法不会改变原字符串，都是返回新字符串，要重新赋值或直接打印
- 数字除法在Python3里默认得到浮点数，想整除用 `//`
- 注释写给自己以后看的，不要不写否则昨天写的今天·可能就忘记了（我每次都嫌麻烦不要学我）
